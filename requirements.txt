Flask
Flask-SQLAlchemy
alpaca-py
python-dotenv
cryptography
requests
werkzeug
gunicorn
numpy
pandas
openpyxl
optuna

# Optional: JIT-accelerates the Keltner backtest (~5-7x). Safe to omit; the
# optimizer falls back to pure Python. Recommended on the Windows runner.
numba
