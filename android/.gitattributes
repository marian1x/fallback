# Keep the Gradle wrapper script LF so it runs on Linux CI runners.
gradlew text eol=lf
*.bat text eol=crlf

# Treat these as binary (never apply EOL/text conversion).
*.jar binary
*.jks binary
*.apk binary
*.aab binary
*.png binary
*.webp binary
*.keystore binary
